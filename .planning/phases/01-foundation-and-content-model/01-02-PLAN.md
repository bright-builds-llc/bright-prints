---
phase: 01-foundation-and-content-model
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - content/creators/peter.yaml
  - content/prints/sample-featured-print/print.yaml
  - content/generators/sign/generator.yaml
  - app/lib/content/schema.ts
  - app/lib/content/load.server.ts
  - app/lib/content/public.ts
  - scripts/validate-content.ts
  - tests/content/content-schema.test.ts
  - app/routes/home.tsx
autonomous: true
requirements:
  - ADMN-01
  - ADMN-03
must_haves:
  truths:
    - "Print and generator content can be authored in repo files and validated against explicit schemas."
    - "Every seeded print and generator record includes creator ownership in a future-multi-creator-friendly shape."
    - "Invalid content fails fast instead of being silently accepted by the app."
  artifacts:
    - path: "app/lib/content/schema.ts"
      provides: "Typed public content schemas for creators, prints, and generators"
    - path: "app/lib/content/load.server.ts"
      provides: "Server-side content loading and validation"
    - path: "scripts/validate-content.ts"
      provides: "Command-line validation entrypoint for repo-backed content"
    - path: "content/prints/sample-featured-print/print.yaml"
      provides: "Seed print content using the new schema"
    - path: "content/generators/sign/generator.yaml"
      provides: "Seed generator content using the new schema"
  key_links:
    - "The validation script parses the same schemas used by the app loader"
    - "Seed print and generator content reference a creator by slug instead of embedding ad hoc owner fields"
    - "The home route consumes the loader output so the content model is proven in real route code"
---

<objective>
Define the repo-backed public content model and prove it works end-to-end inside the app.

Purpose: Lock the authoring shape for prints and generators before discovery work starts, while ensuring creator ownership is represented cleanly from day one.
Output: Public content schemas, loaders, validation command, and seed content wired into the foundation route.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-content-model/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the public content schema and directory structure</name>
  <files>content/creators/peter.yaml, content/prints/sample-featured-print/print.yaml, content/generators/sign/generator.yaml, app/lib/content/schema.ts, app/lib/content/public.ts</files>
  <action>Create the repo-backed public content model using a schema-first approach. Use a structure that keeps creator records separate from print/generator records and references ownership by slug. Include schema versioning, public metadata, and the fields needed by future discovery/detail/generator work, but explicitly exclude private runtime or payment data. Keep the content format easy to diff and easy to validate.</action>
  <verify>Seed content parses through the schema layer without type or runtime validation failures.</verify>
  <done>There is a clear public content structure for creators, prints, and generators with creator ownership represented explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the server-side content loader and validation command</name>
  <files>app/lib/content/load.server.ts, scripts/validate-content.ts, tests/content/content-schema.test.ts</files>
  <action>Build a server-side content loader that reads the repo-backed files, validates them through the shared schema layer, and returns a typed read model for route code. Add a command-line validation script and at least a focused automated test for invalid-content failure behavior. Do not allow the app to proceed silently on malformed content.</action>
  <verify>`pnpm validate:content` passes on seed content and the focused test suite passes.</verify>
  <done>The app has a single typed content loading path and a validation command suitable for local and CI use.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the content pipeline in the foundation route</name>
  <files>app/routes/home.tsx</files>
  <action>Update the minimal home route from Plan `01-01` so it consumes the validated content loader instead of hardcoded placeholders. Keep the output intentionally sparse and foundation-focused: render enough content-derived state to prove the pipeline works, but do not build the polished discovery experience from Phase 2 yet.</action>
  <verify>`pnpm build` passes and the root route renders using validated seed content.</verify>
  <done>The app's foundation route depends on the real content pipeline, proving the schema and loader design in live route code.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes for content-related tests
- [ ] `pnpm validate:content` passes
- [ ] `pnpm build` passes with the route using loaded content
</verification>

<success_criteria>

- All tasks completed
- Repo-backed public content is typed, validated, and creator-aware
- Seed content exists for at least one creator, one print, and one generator
- The foundation route proves the content pipeline works in real route code
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-content-model/01-02-SUMMARY.md`
</output>

