---
phase: 03-print-detail-files-and-launch-actions
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/routes/print-detail.tsx
  - app/routes/print-detail.css
  - app/components/print-detail/PrintHeroActions.tsx
  - app/components/print-detail/PrintFileSections.tsx
  - app/components/print-detail/PrintTrustSection.tsx
  - app/components/print-detail/PrintSpecsSection.tsx
autonomous: true
requirements:
  - PRNT-02
  - FILE-02
  - COMM-01
user_setup: []
must_haves:
  truths:
    - "The print detail route becomes a genuinely useful object page instead of a Phase 2 placeholder entry screen."
    - "The page follows the locked hierarchy: editorial two-column hero, availability and action block near the hero, files immediately after, trust nearby, then deeper print guidance, with related prints clearly secondary."
    - "Print-ready and source file groups stay visible as separate concepts, including inactive or explanatory treatment when a group is unavailable."
    - "Phase 3 print-detail styling is isolated in a dedicated stylesheet instead of silently repurposing the generator-detail shared `item-detail.css` surface."
  artifacts:
    - path: "app/routes/print-detail.tsx"
      provides: "Loader-driven Phase 3 print detail route"
    - path: "app/components/print-detail/PrintFileSections.tsx"
      provides: "Separated Print-Ready Files and Source Files presentation"
    - path: "app/routes/print-detail.css"
      provides: "Print-specific route styling for the Phase 3 detail hierarchy"
  key_links:
    - "The route consumes the read model from Plan 03-01 rather than rebuilding trust, grouping, and availability-aware CTA selection in JSX"
    - "Availability, file access, and trust information appear above deeper technical guidance so the page answers `What can I do with this print right now?` early"
    - "Physical-print and mixed-availability states reserve a lightweight request/contact action near the hero without pretending checkout exists yet"
    - "Print-detail styling is isolated in `app/routes/print-detail.css`, leaving `item-detail.css` as generator-detail's existing surface"
    - "Related prints remain at the bottom and do not compete with the primary file and action area"
---

<objective>
Rebuild the print detail page around the Phase 3 composition decisions.

Purpose: Turn the current lightweight entry page into the first truly useful print page, with the hero, actions, file sections, trust information, and print guidance placed in the order the locked context requires.
Output: A loader-driven Phase 3 print detail route with route-local styling and dedicated print-detail presentation components.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-CONTEXT.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-RESEARCH.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-01-SUMMARY.md
@app/routes/print-detail.tsx
@app/routes/item-detail.css
@app/routes/generator-detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the placeholder print route with the Phase 3 loader and page composition</name>
  <files>app/routes/print-detail.tsx, app/components/print-detail/PrintHeroActions.tsx, app/components/print-detail/PrintSpecsSection.tsx</files>
  <action>Refactor the route loader to resolve the target print and build the dedicated Phase 3 detail model from Plan 03-01. Replace the placeholder copy and generic metadata layout with the locked structure: large visual on the left, title and summary on the right, explicit availability panel, and context-sensitive action area near the hero. Make the hero action stack explicit for the seeded states: `open-source` prioritizes the strongest file-access path, `physical-print` reserves a lightweight request/contact primary action, and `both` keeps the make-it-now path primary while still showing request/contact as a nearby secondary action. Keep the route declarative and loader-driven; the component should render the prepared model, not decide business rules on the fly or introduce Phase 6 order-intent persistence.</action>
  <verify>`pnpm build` passes and the print-detail route renders from the new loader without runtime or type errors, with the correct CTA stack visible for `open-source`, `physical-print`, and `both` seeded states.</verify>
  <done>The print route is no longer a transitional entry page and now exposes the real top-of-page Phase 3 structure.</done>
</task>

<task type="auto">
  <name>Task 2: Render file sections and trust content in the required order</name>
  <files>app/routes/print-detail.tsx, app/components/print-detail/PrintFileSections.tsx, app/components/print-detail/PrintTrustSection.tsx</files>
  <action>Render `Print-Ready Files` first and `Source Files` second, with each row showing label, short purpose text, and file type. Keep unavailable groups visible with honest explanatory treatment instead of hiding them. Place licensing and provenance content near the file area in a dedicated trust section, and show `Unavailable` where the read model says a key trust field is missing.</action>
  <verify>The built route shows separate file groups, visible inactive states, and a trust section near the files for every seeded print state exercised in Plan 03-01.</verify>
  <done>The page explains what files exist, what kind they are, and what trust details are missing or present before the user has to guess.</done>
</task>

<task type="auto">
  <name>Task 3: Apply route-local styling for the final Phase 3 hierarchy</name>
  <files>app/routes/print-detail.css</files>
  <action>Create `app/routes/print-detail.css` and keep the Phase 3 hierarchy there so the page preserves the editorial Bright Prints feel while becoming more practically useful. Keep the top spacious and image-led, then transition into compact structured blocks lower on the page. Ensure the file area is prominent without overwhelming the hero, and keep related prints visibly secondary at the bottom. Do not treat `item-detail.css` as print-local; generator-detail keeps that stylesheet until a separate generator pass intentionally changes it.</action>
  <verify>`pnpm build` passes, the route layout remains coherent on both mobile and desktop breakpoints, and the print-detail polish is isolated to `app/routes/print-detail.css`.</verify>
  <done>The page hierarchy reads clearly and supports the locked Phase 3 editorial-versus-utility balance.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm build` passes
- [ ] The print detail page follows the locked hierarchy, keeps both file groups visible, and shows the correct CTA/request-contact stack for `open-source`, `physical-print`, and `both`
- [ ] Print-detail styling is isolated in `app/routes/print-detail.css`
</verification>

<success_criteria>

- All tasks completed
- Print detail pages expose the Phase 3 hero, context-sensitive actions, files, trust, and guidance structure
- Physical-print-only and mixed-availability states expose an honest lightweight request/contact action without implying checkout
- Print-ready and source assets are clearly separated and always understandable
- The route preserves Bright Prints' editorial feel while answering the user's immediate next action
  </success_criteria>

<output>
After completion, create `.planning/phases/03-print-detail-files-and-launch-actions/03-02-SUMMARY.md`
</output>
