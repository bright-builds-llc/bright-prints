---
phase: 03-print-detail-files-and-launch-actions
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - app/routes.ts
  - app/routes/print-detail.tsx
  - app/routes/print-download.ts
  - app/lib/content/load.server.ts
  - app/lib/prints/download.server.ts
  - app/lib/prints/launch.ts
  - tests/prints/print-download.test.ts
  - tests/prints/print-launch.test.ts
autonomous: true
requirements:
  - PRNT-03
  - FILE-01
  - FILE-02
  - FILE-03
user_setup: []
must_haves:
  truths:
    - "Repo-backed print files download through a server attachment response instead of client-side fetch or browser-hint hacks."
    - "External file destinations stay in the same file lists but are clearly marked as external and keep the user informed about provenance before click."
    - "Slicer launch behavior is allowlisted and vendor-specific, with honest download fallback messaging whenever no supported integration exists."
    - "Download tests use the checked-in `content/prints/<slug>/files/` fixture bytes from Plan 03-01 instead of synthetic stubs or nonexistent `models/...` paths."
    - "Physical-print and mixed-availability states expose a lightweight request/contact action in Phase 3 without pretending Phase 6 order-intent capture already exists."
  artifacts:
    - path: "app/routes/print-download.ts"
      provides: "Server route for repo-backed attachment responses"
    - path: "app/lib/prints/download.server.ts"
      provides: "Safe repo-file resolution for print downloads"
    - path: "app/lib/prints/launch.ts"
      provides: "Allowlisted launch-capability, CTA selection, and lightweight request/contact helpers"
    - path: "tests/prints/print-download.test.ts"
      provides: "Coverage for download routing and attachment behavior"
  key_links:
    - "The print-detail page can link repo files to a server-backed attachment route backed by the checked-in Plan 03-01 fixture bytes, and external files to direct destinations without mixing the two behaviors"
    - "CTA selection uses an explicit capability registry plus availability-aware request/contact rules instead of implying generic `Print now` support from availability alone"
    - "Phase 3 uses a stateless request/contact action for physical availability now, while durable request capture stays deferred to Phase 6"
    - "Download and launch semantics integrate into the Phase 3 route without moving trust or grouping logic back out of the read model"
---

<objective>
Add trustworthy file delivery and launch-action behavior to the Phase 3 print page.

Purpose: Make file access honest and dependable, distinguish internal attachments from external destinations, and support slicer launch only where the app can actually stand behind the behavior.
Output: Server-backed print downloads, explicit external-file handling, and an allowlisted launch-action layer wired into the print detail route.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-CONTEXT.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-RESEARCH.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-01-SUMMARY.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-02-SUMMARY.md
@app/routes.ts
@app/routes/print-detail.tsx
@app/lib/content/load.server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a server-backed download path for repo files</name>
  <files>app/routes.ts, app/routes/print-download.ts, app/lib/content/load.server.ts, app/lib/prints/download.server.ts, tests/prints/print-download.test.ts</files>
  <action>Create a dedicated server route for repo-backed print files that validates the requested print and file, resolves the repo path safely, and returns an attachment response with the correct filename and content type. Use the checked-in fixture bytes created in Plan 03-01 under `content/prints/<slug>/files/` as the byte source for both implementation and tests. Keep the implementation explicit about which file records are eligible for internal download, and reject missing or unsupported file references instead of falling through to unsafe path handling or client-side download tricks.</action>
  <verify>`pnpm test -- tests/prints/print-download.test.ts` and `pnpm build` pass with coverage for successful downloads that assert real fixture bytes and headers, plus invalid slugs and unsupported file references.</verify>
  <done>Repo-backed files are delivered through a trustworthy attachment route instead of ad hoc browser behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add the launch-capability registry and availability-aware CTA rules</name>
  <files>app/lib/prints/launch.ts, tests/prints/print-launch.test.ts</files>
  <action>Implement a small allowlisted launch-capability module keyed by the vendors or file types the app can actually support. Use it plus availability state to determine the primary and secondary CTA contract: launch appears only when explicitly supported, `physical-print` records expose a lightweight request/contact primary action, and `both` records keep the strongest make-it-now path primary while surfacing request/contact as a nearby secondary action. In this phase, make the request/contact path stateless, for example a `mailto:` action seeded from the print title and slug, and defer stored request capture to Phase 6. Do not invent a universal browser-to-slicer protocol or generic `Print now` behavior.</action>
  <verify>`pnpm test -- tests/prints/print-launch.test.ts` and `pnpm typecheck` pass with focused cases for supported-launch, download fallback, `physical-print` request/contact primary behavior, and `both` mixed-availability action selection.</verify>
  <done>The app has an explicit, testable contract for launch, download, and request/contact behavior instead of relying on implication or vague product copy.</done>
</task>

<task type="auto">
  <name>Task 3: Wire download, external, request/contact, and launch actions into the print page honestly</name>
  <files>app/routes/print-detail.tsx</files>
  <action>Update the Phase 3 page so repo-backed files use the new attachment route, external files remain direct links with clear provenance, and launch actions appear only when the capability registry allows them. Wire the lightweight request/contact action from Task 2 into the hero for `physical-print` and `both` states with self-describing copy such as `Request This Print` or `Ask About a Physical Print`. Keep link text self-describing, mark external destinations before click, and pair unsupported launch paths with explicit download fallback messaging rather than letting the UI overpromise.</action>
  <verify>The built route exposes the correct internal download links, safe external links, capability-gated launch behavior, and honest request/contact behavior for the seeded `open-source`, `physical-print`, and `both` Phase 3 prints.</verify>
  <done>Visitors can access files and launch actions with confidence about what will happen next.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes
- [ ] `pnpm build` passes
- [ ] Download tests assert real bytes from checked-in `content/prints/<slug>/files/` fixtures
- [ ] Repo-backed and external file actions are clearly differentiated
- [ ] Physical-print and mixed-availability states expose the planned lightweight request/contact action
</verification>

<success_criteria>

- All tasks completed
- Internal files download through server attachment responses
- External files stay clearly marked and provenance-aware
- Launch actions appear only when explicitly supported and otherwise fall back to download messaging
- Physical-print-only and mixed-availability states expose honest request/contact behavior without overcommitting to checkout
  </success_criteria>

<output>
After completion, create `.planning/phases/03-print-detail-files-and-launch-actions/03-03-SUMMARY.md`
</output>
