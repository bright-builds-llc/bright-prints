---
phase: 04-accounts-and-personal-library
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - app/routes.ts
  - app/root.tsx
  - app/app.css
  - app/routes/library.tsx
  - app/routes/library.css
  - app/lib/library/query.server.ts
  - app/lib/library/model.ts
  - app/lib/content/public.ts
  - app/components/library/LibrarySidebar.tsx
  - app/components/library/LibraryPrintGrid.tsx
  - app/components/library/LibraryListForms.tsx
  - tests/accounts/library-model.test.ts
  - tests/accounts/library-route.test.ts
autonomous: true
requirements:
  - ACCT-03
  - ACCT-04
user_setup: []
must_haves:
  truths:
    - "The library display model is derived from public content plus private list membership keyed by `printSlug`, not from duplicated print metadata in runtime rows."
    - "`Bookmarks` is the default library section and remains visually primary, with custom lists nearby but secondary."
    - "Library management stays lightweight: create, rename, delete custom lists, remove items, and adjust list membership without turning the page into a profile dashboard."
    - "Empty states push the user back into discovery with a clear next click instead of leaving the library as a dead end."
  artifacts:
    - path: "app/lib/library/model.ts"
      provides: "Pure library read model that joins runtime list membership with public print content"
    - path: "app/routes/library.tsx"
      provides: "Loader-driven personal-library route with fetcher-based inline management"
    - path: "app/routes/library.css"
      provides: "Dedicated library styling that keeps `Bookmarks` primary without dashboard sprawl"
    - path: "app/components/library/LibraryPrintGrid.tsx"
      provides: "Saved-print presentation and inline management controls for lists"
  key_links:
    - "The library route builds on mutation helpers and the explicit mutation-route contract from Plan 04-02 instead of re-implementing list writes inside JSX"
    - "Runtime list membership resolves print display from public content helpers through `printSlug`, preserving the public-private boundary"
    - "The root shell can expose a restrained library entry point without turning the app into an account-first product"
    - "Library empty states route back into catalog discovery so saved-print management stays part of the broader product loop"
    - "Wave 3 posts to the locked Wave 2 action intents rather than inventing route names, field names, or response assumptions at the UI layer"
---

<objective>
Build the personal-library read model and management surface for Phase 4.

Purpose: Give signed-in users one calm place to revisit, organize, and manage saved prints while preserving the repo-backed public/private boundary and the discovery-first product stance.
Output: A `/library` route, a pure library read model derived from `printSlug`, and inline list-management UI that keeps `Bookmarks` primary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-accounts-and-personal-library/04-CONTEXT.md
@.planning/phases/04-accounts-and-personal-library/04-RESEARCH.md
@.planning/phases/04-accounts-and-personal-library/04-02-SUMMARY.md
@app/root.tsx
@app/lib/content/public.ts
@app/lib/discovery/model.ts
@app/routes/print-detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the library query layer and pure read model</name>
  <files>app/lib/library/query.server.ts, app/lib/library/model.ts, app/lib/content/public.ts, tests/accounts/library-model.test.ts</files>
  <action>Implement the query and model helpers that load a signed-in user's saved lists and list items, ensure the `Bookmarks` list exists on read, and join each saved `printSlug` back to public print content. Keep the model functional-core style: return the page sections, selected-list data, empty-state copy, and inline-management affordances as data instead of computing them ad hoc inside route components. Handle missing or removed public prints honestly, for example by omitting them from the rendered grid while surfacing enough information to keep the list state understandable.</action>
  <verify>`pnpm test -- tests/accounts/library-model.test.ts` and `pnpm typecheck` pass with coverage for `Bookmarks` ordering, custom-list ordering, empty states, and public-content resolution from `printSlug`.</verify>
  <done>The library has a reusable read model that respects the public/private boundary and keeps `Bookmarks` primary.</done>
</task>

<task type="auto">
  <name>Task 2: Add the `/library` route and inline management UI</name>
  <files>app/routes.ts, app/routes/library.tsx, app/routes/library.css, app/components/library/LibrarySidebar.tsx, app/components/library/LibraryPrintGrid.tsx, app/components/library/LibraryListForms.tsx, tests/accounts/library-route.test.ts</files>
  <action>Add a dedicated `/library` route that requires a signed-in user, loads the Phase 4 library model, and renders `Bookmarks` as the default section with custom lists visibly nearby. Support inline create, rename, delete, remove-item, and add-or-remove membership interactions through fetchers that post to the explicit Wave 2 mutation contracts: `/actions/list-membership` with `intent=create-list|rename-list|delete-list|add-print-to-list|remove-print-from-list` and the required `listId`, `name`, and `printSlug` fields for each case, plus `/actions/save-print` with `intent=save-bookmark|remove-bookmark` for the primary `Bookmarks` action. Interpret the stable action JSON by `intent`, `ok`, and affected identifiers instead of inventing ad hoc response handling in components. Keep the surface focused on saved prints and list organization, not profile metrics or account settings.</action>
  <verify>`pnpm test -- tests/accounts/library-route.test.ts`, `pnpm typecheck`, and `pnpm build` pass, and the route can render `Bookmarks`, custom lists, discovery-forward empty states, and inline management wired against the Wave 2 action contracts without full-page mutation redirects.</verify>
  <done>Signed-in users can view and manage saved bookmarks and custom lists from one dedicated library route.</done>
</task>

<task type="auto">
  <name>Task 3: Add restrained navigation and return paths into the library</name>
  <files>app/root.tsx, app/app.css, app/routes/library.tsx</files>
  <action>Add the minimum app-shell and page-level entry points needed to revisit the library later, such as a restrained current-user utility link and a post-save path from print-detail list organization into `/library`. Keep the navigation subtle and product-oriented: the user should be able to get to the library easily without the app suddenly behaving like an account dashboard.</action>
  <verify>`pnpm build` passes, the app exposes a clear path into `/library` for signed-in users, and the new shell affordances remain visually secondary to the discovery and print surfaces.</verify>
  <done>The library is easy to revisit without taking over the product shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- tests/accounts/library-model.test.ts tests/accounts/library-route.test.ts` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm build` passes
- [ ] The library read model derives saved-print display data from public content via `printSlug`
- [ ] `Bookmarks` is the default library section and custom lists remain nearby but secondary
- [ ] Library fetchers use the explicit Wave 2 route and action contracts for list CRUD, list membership, and bookmark writes
- [ ] Empty states link back to discovery instead of ending the flow
</verification>

<success_criteria>

- All tasks completed
- Signed-in users can view and manage saved bookmarks and custom lists
- Library rendering respects the repo-backed public/private boundary
- The library surface stays calm and management-focused rather than becoming a profile dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/04-accounts-and-personal-library/04-03-SUMMARY.md`
</output>
