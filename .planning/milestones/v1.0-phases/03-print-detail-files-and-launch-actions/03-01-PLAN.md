---
phase: 03-print-detail-files-and-launch-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/content/schema.ts
  - app/lib/content/public.ts
  - app/lib/prints/detail.ts
  - content/prints/sample-featured-print/print.yaml
  - content/prints/sample-featured-print/files/
  - content/prints/modular-cable-clip/print.yaml
  - content/prints/modular-cable-clip/files/
  - content/prints/stackable-desk-sign/print.yaml
  - content/prints/stackable-desk-sign/files/
  - tests/content/content-schema.test.ts
  - tests/prints/print-detail-model.test.ts
autonomous: true
requirements:
  - PRNT-02
  - FILE-02
  - FILE-03
  - COMM-01
user_setup: []
must_haves:
  truths:
    - "Phase 3 uses a dedicated print-detail read model instead of pushing page-specific trust and file logic back into the discovery-item model."
    - "Authored print content stores only durable object facts such as files, license, print guidance, and availability, while grouping, placeholders, and CTA priority remain derived UI state."
    - "Seeded repo-backed file records point at checked-in fixture bytes under `content/prints/<slug>/files/`, not nonexistent `models/...` paths, so attachment behavior can later be verified against real repo assets."
    - "Missing trust-facing fields that are part of the product contract can be represented explicitly as `Unavailable` in the read model instead of disappearing from the page."
  artifacts:
    - path: "app/lib/prints/detail.ts"
      provides: "Pure print-detail read model and grouping helpers for Phase 3"
    - path: "app/lib/content/schema.ts"
      provides: "Phase 3 print content schema for file purpose, licensing, and print-detail facts"
    - path: "content/prints/"
      provides: "Per-print `files/` fixture directories that give repo-backed file records real bytes"
    - path: "tests/prints/print-detail-model.test.ts"
      provides: "Coverage for grouping, placeholder, and CTA-derivation behavior"
  key_links:
    - "The print-detail route can load public content and delegate all file grouping, trust shaping, and availability derivation to a pure helper"
    - "Source-file versus print-ready distinction is derived from validated file records, not handwritten route conditionals"
    - "Plan 03-03 can serve actual repo bytes from seeded `content/prints/<slug>/files/` fixtures instead of stalling on nonexistent `models/...` references"
    - "Seeded print records exercise the real combinations Phase 3 must support: open-source only, physical-print only, both, missing trust fields, and mixed file provenance"
---

<objective>
Create the print-detail data and trust foundation for Phase 3.

Purpose: Replace the Phase 2 placeholder assumptions with authored print facts plus a pure read model that can drive the final detail page, file groups, availability messaging, and trust placeholders.
Output: Expanded print content schema, richer seeded print records, and a tested print-detail read model.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-CONTEXT.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-RESEARCH.md
@app/lib/content/schema.ts
@app/lib/content/public.ts
@app/routes/print-detail.tsx
@content/prints/sample-featured-print/print.yaml
@content/prints/modular-cable-clip/print.yaml
@content/prints/stackable-desk-sign/print.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand authored print content only where Phase 3 needs real facts</name>
  <files>app/lib/content/schema.ts, content/prints/sample-featured-print/print.yaml, content/prints/sample-featured-print/files/, content/prints/modular-cable-clip/print.yaml, content/prints/modular-cable-clip/files/, content/prints/stackable-desk-sign/print.yaml, content/prints/stackable-desk-sign/files/, tests/content/content-schema.test.ts</files>
  <action>Extend the print schema for the durable facts the locked context actually requires: license/trust data, short file-purpose text, and richer print-detail guidance such as material, layer height, and special steps. Keep vendor launch capability, file grouping order, placeholders, and CTA ranking out of authored content. Update the seeded Phase 3 print records so the repo exercises the combinations the page must explain, including repo-backed files, external files, print-ready versus source assets, and explicit missing trust fields that should surface as `Unavailable` later. Create a small checked-in byte fixture subtree under `content/prints/<slug>/files/` and repoint any seeded `repoPath` values away from nonexistent `models/...` locations so later download work can serve real repo bytes without inventing assets on the fly.</action>
  <verify>`pnpm validate:content` and `pnpm test -- tests/content/content-schema.test.ts` pass against the expanded schema and seed content, and every seeded `repoPath` used for Phase 3 points at a checked-in fixture file under `content/prints/<slug>/files/`.</verify>
  <done>The repo has enough authored print facts and byte-backed fixture files to build a trustworthy detail page without smuggling UI policy into YAML.</done>
</task>

<task type="auto">
  <name>Task 2: Build the pure print-detail read model</name>
  <files>app/lib/content/public.ts, app/lib/prints/detail.ts, tests/prints/print-detail-model.test.ts</files>
  <action>Create a dedicated print-detail module that accepts validated content and a print record, then returns the Phase 3 page model: hero facts, availability panel content, grouped file sections, trust fields with placeholder handling, print guidance blocks, derived action intents for `open-source`, `physical-print`, and `both`, and related prints. Keep this module functional-core style so route components only render the returned structure. Do not extend the discovery model into a second job it was not designed to own.</action>
  <verify>`pnpm test -- tests/prints/print-detail-model.test.ts` and `pnpm typecheck` pass with focused coverage for grouping, placeholder behavior, and action-priority selection.</verify>
  <done>The route can ask one tested helper what the print-detail page should show instead of recomputing Phase 3 rules inline.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the model can express trust gaps without hiding them</name>
  <files>app/lib/prints/detail.ts, tests/prints/print-detail-model.test.ts</files>
  <action>Add explicit cases for missing license or file-provenance details, unavailable file groups, and availability combinations that should remain visible to visitors, including physical-print-only and mixed-availability states that need a lightweight request/contact intent without implying Phase 6 order capture. Make the read model return stable structures for these cases so later route work can render `Unavailable`, explanatory inactive states, and honest CTA hierarchy without branching all over JSX.</action>
  <verify>The print-detail model tests prove that missing trust fields, unavailable groups, and the Phase 3 request/contact-versus-download action states stay visible in the returned structure.</verify>
  <done>The Phase 3 UI has a stable contract for honest trust, availability, and CTA messaging, even when authored content is incomplete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm validate:content` passes
- [ ] `pnpm test` passes
- [ ] `pnpm typecheck` passes
- [ ] Seeded repo-backed file records point at checked-in `content/prints/<slug>/files/` fixtures
- [ ] The read model covers grouped files, trust placeholders, and availability/CTA derivation
</verification>

<success_criteria>

- All tasks completed
- Print-detail authored facts are explicit and scoped to Phase 3
- File grouping and trust handling live in a tested pure helper
- Missing trust fields can surface as `Unavailable` instead of being hidden
  </success_criteria>

<output>
After completion, create `.planning/phases/03-print-detail-files-and-launch-actions/03-01-SUMMARY.md`
</output>
