---
phase: 03-print-detail-files-and-launch-actions
plan: 04
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - app/routes/print-detail.tsx
  - app/routes/print-detail.css
  - app/components/print-detail/PrintFileSections.tsx
  - app/components/print-detail/PrintTrustSection.tsx
autonomous: false
requirements:
  - PRNT-02
  - PRNT-03
  - FILE-01
  - FILE-02
  - FILE-03
  - COMM-01
user_setup: []
must_haves:
  truths:
    - "The final print-detail experience feels trustworthy and understandable without relying on color, hover state, or layout context to explain files and actions."
    - "Keyboard flow, focus order, link naming, and unavailable-state messaging remain clear across the hero, file lists, trust section, and related-print area."
    - "Related prints and lower-page polish stay clearly secondary to the main purpose of the page: understanding the object, availability, and file actions."
    - "Final print-detail polish stays in the isolated print stylesheet so generator-detail does not regress as a side effect of Phase 3 cleanup."
  artifacts:
    - path: "app/routes/print-detail.tsx"
      provides: "Final integrated Phase 3 print-detail experience"
    - path: "app/routes/print-detail.css"
      provides: "Responsive polish for the final detail-page hierarchy"
    - path: "app/components/print-detail/PrintTrustSection.tsx"
      provides: "Final trust/provenance presentation"
  key_links:
    - "Download and launch actions from Plan 03-03 remain semantically understandable after the final polish pass"
    - "The file and trust sections still honor the locked ordering and placeholder rules after responsive refinement"
    - "Physical-print-only and mixed-availability request/contact actions stay obvious and honest after the polish pass"
    - "Final responsive work stays in `app/routes/print-detail.css` instead of mutating generator-detail's shared stylesheet"
    - "The Phase 3 page preserves Bright Prints' editorial identity while prioritizing trust and next actions"
---

<objective>
Polish, audit, and verify the completed Phase 3 print-detail journey.

Purpose: Confirm that the final Phase 3 page is not just feature-complete but also accessible, responsive, and trustworthy in the way it communicates files, availability, and actions.
Output: Final accessibility and cohesion pass plus a human verification checkpoint for the completed print-detail experience.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
@/Users/peterryszkiewicz/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-CONTEXT.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-02-SUMMARY.md
@.planning/phases/03-print-detail-files-and-launch-actions/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit semantics, focus flow, and trust clarity</name>
  <files>app/routes/print-detail.tsx, app/components/print-detail/PrintFileSections.tsx, app/components/print-detail/PrintTrustSection.tsx</files>
  <action>Do a deliberate semantics pass over the completed page. Tighten link and action text so file destinations make sense in context, ensure external destinations are marked before click, confirm unavailable states remain understandable without color alone, and make sure the trust section stays visible instead of collapsing into decorative metadata. Verify that `physical-print` pages still present a lightweight request/contact action, and that mixed-availability pages keep request/contact secondary to the strongest make-it-now action without ambiguous copy. Fix keyboard and focus-order issues created during the earlier composition and download work.</action>
  <verify>`pnpm build` passes and the route can be tabbed through in a predictable order with self-describing download, launch, and request/contact labels.</verify>
  <done>The page communicates file access and trust information clearly even when layout or color cues are reduced.</done>
</task>

<task type="auto">
  <name>Task 2: Finish responsive polish and related-print restraint</name>
  <files>app/routes/print-detail.tsx, app/routes/print-detail.css</files>
  <action>Refine spacing, breakpoint behavior, and lower-page composition so the hero, actions, file area, trust content, and technical guidance stay readable on narrow and wide screens. Keep related prints useful but clearly secondary, and avoid letting the bottom of the page pull attention away from the core Phase 3 tasks near the top. Keep this polish in `app/routes/print-detail.css`; do not reach back into `item-detail.css` unless a separate generator regression check is intentionally added.</action>
  <verify>`pnpm build` passes, the final detail layout remains coherent across mobile and desktop breakpoints, and the final CSS polish stays isolated to `app/routes/print-detail.css`.</verify>
  <done>The final print-detail experience feels intentional and stable on the screen sizes the repo currently targets.</done>
</task>

<task type="auto">
  <name>Task 3: Start the app for final review</name>
  <files>none</files>
  <action>Run the development or production-start server in the background and confirm representative Phase 3 print detail pages are reachable before the human verification checkpoint begins.</action>
  <verify>HTTP checks confirm the reviewed print-detail routes are reachable.</verify>
  <done>The app is running and ready for human verification of the completed Phase 3 journey.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 3 print detail experience — useful print pages, grouped files, trust content, availability messaging, downloads, and gated launch actions</what-built>
  <how-to-verify>Visit a few print detail pages and check: the hero makes the print's availability and next action obvious, `Print-Ready Files` and `Source Files` are clearly distinct, physical-print-only pages show a lightweight request/contact action, mixed-availability pages keep request/contact secondary to the primary make-it-now path, external destinations are marked before click, unsupported launch paths fall back honestly to download messaging, trust fields stay visible with `Unavailable` where needed, and related prints remain secondary to the main file/action area.</how-to-verify>
  <resume-signal>Type "approved" or describe the issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes
- [ ] `pnpm build` passes
- [ ] Final CSS polish stayed in `app/routes/print-detail.css`
- [ ] Human visual verification passed
</verification>

<success_criteria>

- All tasks completed
- File and action semantics remain understandable and trustworthy
- Physical-print-only and mixed-availability request/contact actions remain obvious and honest after the polish pass
- Responsive polish preserves the locked Phase 3 hierarchy
- User confirms the completed Phase 3 print-detail experience satisfies the locked decisions
  </success_criteria>

<output>
After completion, create `.planning/phases/03-print-detail-files-and-launch-actions/03-04-SUMMARY.md`
</output>
