---
phase: 02-discovery-and-catalog-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/content/schema.ts
  - app/lib/content/public.ts
  - app/lib/content/load.server.ts
  - app/lib/discovery/model.ts
  - app/lib/discovery/filter.ts
  - app/components/discovery/DiscoveryBadge.tsx
  - app/components/discovery/DiscoveryCard.tsx
  - app/components/discovery/SectionHeading.tsx
  - content/prints/sample-featured-print/print.yaml
  - content/prints/modular-cable-clip/print.yaml
  - content/prints/stackable-desk-sign/print.yaml
  - content/generators/sign/generator.yaml
  - tests/discovery/discovery-model.test.ts
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - PRNT-01
user_setup: []
must_haves:
  truths:
    - "The repository contains enough seeded discovery content to support a believable featured home page and a real unified catalog."
    - "Home and catalog routes can consume a normalized discovery-item model that mixes prints and generators without flattening the underlying content model."
    - "Shared discovery presentation primitives exist so home and catalog can stay visually coherent."
  artifacts:
    - path: "app/lib/discovery/model.ts"
      provides: "Unified discovery-item and featured-home read model helpers"
    - path: "app/lib/discovery/filter.ts"
      provides: "Pure search/filter/sort helpers for the unified catalog"
    - path: "app/components/discovery/DiscoveryCard.tsx"
      provides: "Shared editorial discovery card primitive"
    - path: "tests/discovery/discovery-model.test.ts"
      provides: "Coverage for discovery normalization and filter behavior"
  key_links:
    - "Content loader output feeds the discovery model helpers instead of routes parsing raw content files directly"
    - "The same shared card and badge primitives can be used by both home and catalog surfaces"
    - "Search/filter behavior is implemented in pure helpers so route loaders can call it with URL-derived query state"
---

<objective>
Create the shared discovery data and UI foundation for Phase 2.

Purpose: Give the home page and catalog routes a common model and visual language before route-specific work begins.
Output: Expanded discovery seed content, normalized discovery helpers, and shared discovery UI primitives.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-discovery-and-catalog-experience/02-CONTEXT.md
@.planning/phases/02-discovery-and-catalog-experience/02-RESEARCH.md
@app/lib/content/schema.ts
@app/lib/content/public.ts
@app/lib/content/load.server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the seeded discovery content and discovery-facing schema</name>
  <files>app/lib/content/schema.ts, content/prints/sample-featured-print/print.yaml, content/prints/modular-cable-clip/print.yaml, content/prints/stackable-desk-sign/print.yaml, content/generators/sign/generator.yaml</files>
  <action>Extend the public content shape only where Phase 2 actually needs more discovery-facing data, such as ordering or lightweight discovery metadata. Seed enough additional prints and generators so the editorial home page and unified catalog have real variety. Keep the data public, repo-backed, and creator-aware. Do not smuggle Phase 3 detail-depth or Phase 6 commerce complexity into the schema just because the file is open.</action>
  <verify>`pnpm validate:content` passes on the expanded content set.</verify>
  <done>Discovery has enough real content and metadata to support featured home sections and unified catalog behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Build a normalized discovery read model and filter helpers</name>
  <files>app/lib/content/public.ts, app/lib/content/load.server.ts, app/lib/discovery/model.ts, app/lib/discovery/filter.ts, tests/discovery/discovery-model.test.ts</files>
  <action>Create a dedicated discovery layer on top of the raw content loader. It should normalize prints and generators into a shared item shape, derive featured home slices, and expose pure helpers for search/filter/sort driven by query state. Keep route code thin: loaders should call this layer instead of reimplementing discovery rules inline.</action>
  <verify>`pnpm test` and `pnpm typecheck` pass with focused discovery-model coverage.</verify>
  <done>The app has a reusable, tested discovery layer that both route surfaces can share.</done>
</task>

<task type="auto">
  <name>Task 3: Create shared discovery presentation primitives</name>
  <files>app/components/discovery/DiscoveryBadge.tsx, app/components/discovery/DiscoveryCard.tsx, app/components/discovery/SectionHeading.tsx</files>
  <action>Create the core presentation primitives that Phase 2’s route work will reuse. The card should reflect the locked decisions: strong visual hierarchy, one dominant click target, minimal metadata, and calm interaction behavior. Keep these primitives flexible enough for both the editorial home page and the unified catalog without building a generic design system abstraction for its own sake.</action>
  <verify>`pnpm build` passes and the primitives are importable from route modules without circular dependencies.</verify>
  <done>Home and catalog implementation can proceed without inventing separate card systems.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes
- [ ] `pnpm validate:content` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- Discovery data is rich enough to support the Phase 2 journey
- Search/filter rules exist as reusable pure helpers
- Shared discovery UI primitives are ready for both home and catalog routes
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-and-catalog-experience/02-01-SUMMARY.md`
</output>
