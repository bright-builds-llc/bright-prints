---
phase: 02-discovery-and-catalog-experience
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/routes.ts
  - app/routes/catalog.tsx
  - app/routes/catalog.css
  - app/routes/print-detail.tsx
  - app/routes/generator-detail.tsx
  - app/routes/item-detail.css
  - app/components/discovery/CatalogToolbar.tsx
  - app/components/discovery/CatalogEmptyState.tsx
  - tests/discovery/catalog-routing.test.ts
autonomous: true
requirements:
  - DISC-02
  - DISC-03
  - PRNT-01
user_setup: []
must_haves:
  truths:
    - "Visitors can browse a unified catalog containing both prints and generators."
    - "Search, filtering, and sorting use a lightweight top-toolbar model driven by URL query state."
    - "Every catalog item links into a dedicated entry page that establishes the object and supports onward navigation."
  artifacts:
    - path: "app/routes/catalog.tsx"
      provides: "Unified catalog route"
    - path: "app/routes/print-detail.tsx"
      provides: "Dedicated print entry route"
    - path: "app/routes/generator-detail.tsx"
      provides: "Dedicated generator entry route"
    - path: "app/components/discovery/CatalogToolbar.tsx"
      provides: "Search and filter toolbar"
  key_links:
    - "Catalog route consumes the shared discovery filter helpers from Plan 02-01"
    - "Discovery cards use one dominant click target to route into the item entry pages"
    - "Query parameters directly drive the route loader’s search/filter behavior"
---

<objective>
Build the unified catalog and entry-page routing layer for Phase 2.

Purpose: Deliver the actual browse/filter/click-through journey that moves visitors from catalog browsing into dedicated item pages.
Output: Unified catalog route, compact search/filter toolbar, and dedicated print/generator entry routes.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-discovery-and-catalog-experience/02-CONTEXT.md
@.planning/phases/02-discovery-and-catalog-experience/02-RESEARCH.md
@.planning/phases/02-discovery-and-catalog-experience/02-01-SUMMARY.md
@app/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the catalog and item-entry routes</name>
  <files>app/routes.ts, app/routes/catalog.tsx, app/routes/print-detail.tsx, app/routes/generator-detail.tsx</files>
  <action>Create the route structure needed for the Phase 2 journey: a unified catalog route plus dedicated entry pages for print and generator items. Keep the detail-entry routes intentionally scoped to this phase: enough identity, framing, and onward navigation to justify the click, but not the richer technical detail that Phase 3 and Phase 5 own.</action>
  <verify>`pnpm build` passes and each route can be resolved by the router without missing-module or loader errors.</verify>
  <done>The app has real destination routes for catalog browsing and item entry.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the unified catalog toolbar and discovery grid</name>
  <files>app/routes/catalog.tsx, app/routes/catalog.css, app/components/discovery/CatalogToolbar.tsx, app/components/discovery/CatalogEmptyState.tsx, tests/discovery/catalog-routing.test.ts</files>
  <action>Build the catalog around the locked interaction model: a spacious unified grid, top-toolbar search and filters, minimal visible sort control, and one dominant click target per card. Drive search/filter/sort from route query params, not hidden client-only state. Add focused tests for the catalog’s routing/filter behavior where practical.</action>
  <verify>`pnpm test`, `pnpm typecheck`, and `pnpm build` all pass; catalog route behavior can be exercised through query-param-driven tests or equivalent route-level checks.</verify>
  <done>The catalog route satisfies the search/filter/browse requirements and stays aligned with the agreed editorial density.</done>
</task>

<task type="auto">
  <name>Task 3: Make the entry pages feel worth opening</name>
  <files>app/routes/print-detail.tsx, app/routes/generator-detail.tsx, app/routes/item-detail.css</files>
  <action>Implement the first-pass item entry pages so clicking a card feels like entering the object rather than falling into a dead end. Use calm transitions, concise framing, and enough content-derived identity to support the user’s next move. Keep both print and generator entry pages lightweight and Phase 2-scoped; do not leak Phase 3 technical detail or Phase 5 generator controls into these routes yet.</action>
  <verify>Run the built app locally and confirm that catalog cards route into dedicated item pages with no broken links or generic placeholders.</verify>
  <done>The full catalog → item-entry path is real and coherent.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes
- [ ] `pnpm build` passes
- [ ] Catalog query-state behavior works and all catalog cards resolve to dedicated routes
</verification>

<success_criteria>

- All tasks completed
- Unified catalog exists with lightweight search/filter behavior
- Detail-entry routes exist for catalog items
- The Phase 2 browse-to-click-through journey is functionally real
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-and-catalog-experience/02-03-SUMMARY.md`
</output>

