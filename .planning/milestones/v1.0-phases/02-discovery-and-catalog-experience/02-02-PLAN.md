---
phase: 02-discovery-and-catalog-experience
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/routes/home.tsx
  - app/routes/home.css
  - app/components/discovery/HomeHero.tsx
  - app/components/discovery/HomeFeaturedSection.tsx
  - app/components/discovery/ProductPositioningBand.tsx
  - app/components/discovery/GeneratorSpotlight.tsx
autonomous: true
requirements:
  - DISC-01
user_setup: []
must_haves:
  truths:
    - "The home page presents an editorial asymmetric hero instead of a carousel or generic storefront block."
    - "The page clearly positions Bright Prints around open-source prints plus customizable generators, with storefront language kept secondary."
    - "The home page contains the restrained set of sections agreed in context: featured prints, a short explanation band, and a generator spotlight."
  artifacts:
    - path: "app/routes/home.tsx"
      provides: "Home route loader and section composition"
    - path: "app/components/discovery/HomeHero.tsx"
      provides: "Editorial hero implementation with the locked CTA structure"
    - path: "app/components/discovery/GeneratorSpotlight.tsx"
      provides: "Generator spotlight section"
  key_links:
    - "The home route consumes the shared discovery model from Plan 02-01 rather than inventing route-local content shaping"
    - "Home sections reuse the shared discovery card/badge primitives where appropriate"
    - "The locked CTA structure routes visitors toward the catalog and generator journey directly"
---

<objective>
Implement the editorial Phase 2 home page experience.

Purpose: Turn the current foundation route into a deliberate visitor entry point that sells the discovery journey without turning the home page into a dense storefront.
Output: A composed editorial home page aligned with the locked Phase 2 decisions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-discovery-and-catalog-experience/02-CONTEXT.md
@.planning/phases/02-discovery-and-catalog-experience/02-RESEARCH.md
@.planning/phases/02-discovery-and-catalog-experience/02-01-SUMMARY.md
@app/routes/home.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the foundation route with the editorial home composition</name>
  <files>app/routes/home.tsx, app/components/discovery/HomeHero.tsx, app/components/discovery/HomeFeaturedSection.tsx, app/components/discovery/ProductPositioningBand.tsx, app/components/discovery/GeneratorSpotlight.tsx</files>
  <action>Transform the existing foundation route into the actual Phase 2 home page. Use the locked structure from context: editorial asymmetric hero, two CTAs only (`Browse Prints`, `Try a Generator`), featured prints, concise product explanation band, and generator spotlight. Keep the content selective and confident rather than exhaustive. Do not collapse into a generic marketing-site section stack.</action>
  <verify>`pnpm build` passes and the root route renders the expected home sections without loader/runtime errors.</verify>
  <done>The home page now matches the agreed editorial composition and product positioning.</done>
</task>

<task type="auto">
  <name>Task 2: Add route-local styling for the home experience</name>
  <files>app/routes/home.css</files>
  <action>Create route-local styling that reinforces the editorial feel without polluting the global shell more than necessary. Keep the motion subtle, the hierarchy image-led, and the spacing intentional. The implementation should preserve accessibility and calm interaction behavior rather than relying on theatrical transitions.</action>
  <verify>The home route styling loads correctly in production build output and does not break the existing shell layout.</verify>
  <done>The home route has its own coherent visual system aligned with the phase context.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure the home page creates the right next click</name>
  <files>app/routes/home.tsx</files>
  <action>Check that the copy, card usage, and CTA placement drive the visitor into the unified catalog or generator path exactly as the context requires. Tighten any sections that feel too verbose, too storefront-heavy, or too utility-dense. The goal is to make the next click obvious without shouting.</action>
  <verify>Run the built app locally and confirm the home page exposes the two intended primary actions and all three required supporting sections.</verify>
  <done>The home page not only looks correct but actively funnels the user into the Phase 2 discovery journey.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm build` passes
- [ ] Root route exposes the required sections and CTA structure
</verification>

<success_criteria>

- All tasks completed
- Home page matches the locked Phase 2 composition decisions
- Messaging emphasizes open-source prints and generators correctly
- The page creates a strong path into the catalog/detail journey
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-and-catalog-experience/02-02-SUMMARY.md`
</output>

