---
phase: 04-accounts-and-personal-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - .env.example
  - app/lib/env.server.ts
  - app/lib/db.server.ts
  - app/lib/auth/password.server.ts
  - app/lib/auth/session.server.ts
  - app/lib/auth/user.server.ts
  - app/routes.ts
  - app/routes/account.tsx
  - app/routes/account.css
  - app/root.tsx
  - tests/server/env.server.test.ts
  - tests/accounts/auth-session.test.ts
  - tests/accounts/auth-route.test.ts
autonomous: true
requirements:
  - ACCT-01
user_setup: []
must_haves:
  truths:
    - "Phase 4 keeps browsing public and introduces identity only when save or list actions require it."
    - "Accounts use app-owned email-and-password auth plus database-backed sessions instead of a third-party auth suite or browser-only state."
    - "Pending mutation intent and `returnTo` state survive the auth redirect in server-managed session data and can be consumed after sign-in or account creation."
    - "The pending-intent contract is generic enough to represent bookmark save or remove plus custom-list create, rename, delete, add-print, and remove-print mutations without storing UI-only state."
    - "The root route can expose current-user session state for later save and library surfaces without introducing a profile dashboard shell."
  artifacts:
    - path: "app/lib/auth/session.server.ts"
      provides: "Database-backed session primitives, generic pending-intent helpers, and current-user lookup"
    - path: "app/lib/auth/password.server.ts"
      provides: "App-owned password hashing and verification helpers"
    - path: "app/routes/account.tsx"
      provides: "Dedicated sign-in and create-account route with server action handling"
    - path: "prisma/schema.prisma"
      provides: "Phase 4 auth fields and session constraints for app-owned accounts"
  key_links:
    - "The account route consumes the session helpers from `app/lib/auth/session.server.ts` instead of embedding cookie logic inline"
    - "The root route exposes current-user session state that later save controls can read without re-inventing auth checks in every component"
    - "Pending intent stays on the server so a sign-out redirect from bookmark or list mutations can resume the exact original action later"
    - "Wave 1 defines the replayable pending-intent union and auth handoff that Wave 2 extends with concrete list-mutation executors instead of special-casing bookmarks in the account route"
    - "Environment parsing and Prisma runtime access remain server-only through `app/lib/env.server.ts` and `app/lib/db.server.ts`"
---

<objective>
Create the auth and session foundation for Phase 4.

Purpose: Add app-owned email-and-password auth plus database-backed sessions in the existing React Router route-module pattern so save and library work can gate on a real current user without gating browsing.
Output: Auth-capable runtime models, server-only auth/session helpers, a dedicated account route, and root-level session plumbing for downstream save flows.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-accounts-and-personal-library/04-CONTEXT.md
@.planning/phases/04-accounts-and-personal-library/04-RESEARCH.md
@prisma/schema.prisma
@app/routes.ts
@app/root.tsx
@app/lib/db.server.ts
@app/lib/env.server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the runtime auth contract for app-owned accounts</name>
  <files>prisma/schema.prisma, prisma/migrations/, .env.example, app/lib/env.server.ts, tests/server/env.server.test.ts</files>
  <action>Add the minimum Phase 4 auth data and env contract needed for app-owned email-and-password auth with database-backed sessions. Extend the `User` model with a durable password-hash field, tighten any session indexes the helpers will need, and add the env parsing and documentation for a signed session cookie secret. Keep the schema scoped to Phase 4 primitives only; do not add password-reset, MFA, profile, or commerce fields.</action>
  <verify>`pnpm prisma validate` passes, `pnpm test -- tests/server/env.server.test.ts` passes, and the updated env contract documents the session-secret requirement without exposing real values.</verify>
  <done>The repo has a concrete runtime contract for app-owned auth and signed database-backed sessions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement password, session, and pending-intent server helpers</name>
  <files>app/lib/db.server.ts, app/lib/auth/password.server.ts, app/lib/auth/session.server.ts, app/lib/auth/user.server.ts, tests/accounts/auth-session.test.ts</files>
  <action>Create server-only helpers for password hashing and verification, session creation and lookup backed by the Prisma `Session` model, session cookie commit and destroy, and typed pending-intent plus `returnTo` helpers for signed-out write flows. Define a replayable pending-intent union that stores only the server-action payload needed to resume `save-bookmark`, `remove-bookmark`, `create-list`, `rename-list`, `delete-list`, `add-print-to-list`, and `remove-print-from-list` mutations after auth. Keep business rules readable and testable: parse route form input into clear intent shapes, centralize current-user lookup, and keep cookie/DB IO in thin adapters around those helpers.</action>
  <verify>`pnpm test -- tests/accounts/auth-session.test.ts` and `pnpm typecheck` pass with focused coverage for password verification, current-user lookup, session expiry handling, and pending-intent serialization for both bookmark and list-mutation variants.</verify>
  <done>The app has one server-only path for auth primitives and generic pending-intent storage instead of spreading password, session, and resume logic through route modules.</done>
</task>

<task type="auto">
  <name>Task 3: Add the dedicated account route and root session loader</name>
  <files>app/routes.ts, app/routes/account.tsx, app/routes/account.css, app/root.tsx, tests/accounts/auth-route.test.ts</files>
  <action>Add a lightweight `/account` route with sign-in and create-account modes handled through a route action, explicit email/password labels, and clear inline error states. On success, create the database-backed session, call a shared pending-intent replay path that can dispatch the stored mutation after auth, clear the session intent state, and redirect back to the originating print, discovery, or library surface via `returnTo`. In Wave 1, wire this replay path through the generic pending-intent contract so later bookmark and list routes can hand off exact mutation payloads without adding special resume logic to the account route. Add a root loader that exposes current-user session state and any post-auth flash data for downstream save surfaces, but do not turn the app shell into a dashboard.</action>
  <verify>`pnpm test -- tests/accounts/auth-route.test.ts`, `pnpm typecheck`, and `pnpm build` pass, and the account route can create a session, sign in an existing user, replay a stored pending mutation through the shared handoff path, and redirect back to a stored `returnTo` location.</verify>
  <done>The app has a real account entrypoint and a reusable root session surface ready for Phase 4 bookmark and list-resume flows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm prisma validate` passes
- [ ] `pnpm test -- tests/server/env.server.test.ts tests/accounts/auth-session.test.ts tests/accounts/auth-route.test.ts` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm build` passes
- [ ] Account creation and sign-in use database-backed sessions
- [ ] Pending bookmark and list-mutation intent data plus `returnTo` can survive the auth redirect through server-managed session state
</verification>

<success_criteria>

- All tasks completed
- Visitors can create an account and sign in through a dedicated Phase 4 route
- Session and generic pending-intent handling live in server-only helpers instead of route-local ad hoc logic
- Later save and library work can read current-user state from the root session surface without gating browsing
</success_criteria>

<output>
After completion, create `.planning/phases/04-accounts-and-personal-library/04-01-SUMMARY.md`
</output>
