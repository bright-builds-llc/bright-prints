---
phase: 04-accounts-and-personal-library
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - app/root.tsx
  - app/routes.ts
  - app/routes/save-print.ts
  - app/routes/list-membership.ts
  - app/lib/auth/session.server.ts
  - app/lib/library/lists.server.ts
  - app/lib/library/mutations.server.ts
  - app/components/discovery/DiscoveryCard.tsx
  - app/components/library/SavePrintButton.tsx
  - app/components/print-detail/PrintSaveActions.tsx
  - app/routes/print-detail.tsx
  - tests/accounts/library-mutations.test.ts
  - tests/accounts/save-actions.test.ts
autonomous: true
requirements:
  - ACCT-02
  - ACCT-03
user_setup: []
must_haves:
  truths:
    - "Bookmarks is a fixed system list backed by a real database row, with a one-per-user invariant that the app enforces in storage and service code."
    - "Bookmark and list writes are gated in server actions, not in client-only conditionals, so signed-out submissions redirect into auth consistently."
    - "Signed-out bookmark, list-CRUD, and list-membership submissions all serialize into the same pending-intent contract from Plan 04-01 so auth resume does not stop at bookmark saves."
    - "Discovery-card and print-detail save actions stay in place through fetchers and explicit saved-state labels instead of redirecting or relying on icon-only toggles."
    - "The print-detail page saves to `Bookmarks` first and treats custom-list organization as a nearby but secondary action."
    - "Wave 2 defines a stable route-action contract for `/actions/save-print` and `/actions/list-membership` so Wave 3 can wire the library UI without inventing new APIs."
  artifacts:
    - path: "app/lib/library/mutations.server.ts"
      provides: "Transactional bookmark, membership, and custom-list mutation helpers"
    - path: "app/routes/save-print.ts"
      provides: "Auth-gated route action for bookmark save and remove flows with a stable form and response contract"
    - path: "app/routes/list-membership.ts"
      provides: "Auth-gated route action for list CRUD and membership writes with stable `intent` contracts"
    - path: "app/components/library/SavePrintButton.tsx"
      provides: "Reusable fetcher-based save control with explicit text state"
  key_links:
    - "Mutation routes use the session and pending-intent helpers from Plan 04-01 instead of inventing a second auth gate"
    - "The `Bookmarks` row invariant lives in Prisma constraints plus transaction-backed helpers, not in UI assumptions"
    - "Discovery cards and print detail read root or route loader state to label save controls honestly before any mutation happens"
    - "Save and list writes stay within the React Router route-module pattern through route actions and fetchers"
    - "Wave 2 locks the mutation payload shapes and response semantics that Wave 3 library fetchers must post to and interpret"
---

<objective>
Add bookmark and custom-list writes to the discovery and print-detail surfaces.

Purpose: Make signed-in save behavior real, resilient, and in-place while preserving the discovery-first product stance and deferring the heavier library read surface to the next wave.
Output: Fixed `Bookmarks` invariant, auth-gated save and list mutation routes, and fetcher-driven save controls on discovery and print-detail pages.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-accounts-and-personal-library/04-CONTEXT.md
@.planning/phases/04-accounts-and-personal-library/04-RESEARCH.md
@.planning/phases/04-accounts-and-personal-library/04-01-SUMMARY.md
@prisma/schema.prisma
@app/root.tsx
@app/components/discovery/DiscoveryCard.tsx
@app/routes/print-detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce the fixed `Bookmarks` list invariant and list-write services</name>
  <files>prisma/schema.prisma, prisma/migrations/, app/lib/library/lists.server.ts, app/lib/library/mutations.server.ts, tests/accounts/library-mutations.test.ts</files>
  <action>Add the storage-level and service-level guarantees that Phase 4 needs for saved prints: a one-per-user `BOOKMARKS` list invariant, transactional `ensureBookmarksList` behavior, and custom-list create, rename, delete, add-item, and remove-item helpers that keep `Bookmarks` non-removable. Keep runtime rows slim by storing only `printSlug` membership and list metadata; do not denormalize public print content into Prisma. Guard against duplicate rows and racey double submits with transaction-backed upsert or delete logic.</action>
  <verify>`pnpm prisma validate` and `pnpm test -- tests/accounts/library-mutations.test.ts` pass with focused coverage for bookmarks creation, duplicate-save deduping, non-removable `BOOKMARKS`, and custom-list membership writes.</verify>
  <done>The database and service layer can enforce Phase 4 bookmark and list rules without relying on UI discipline.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth-gated save and list-mutation route modules</name>
  <files>app/routes.ts, app/routes/save-print.ts, app/routes/list-membership.ts, app/lib/auth/session.server.ts, tests/accounts/save-actions.test.ts</files>
  <action>Create dedicated route-module actions for bookmark and list writes, and make their contracts explicit. Register `/actions/save-print` for `intent=save-bookmark|remove-bookmark` with required `printSlug` and optional `returnTo`, and register `/actions/list-membership` for `intent=create-list|rename-list|delete-list|add-print-to-list|remove-print-from-list` with these required fields: `create-list` needs `name`; `rename-list` needs `listId` and `name`; `delete-list` needs `listId`; `add-print-to-list` and `remove-print-from-list` need `listId` and `printSlug`; every action may include `returnTo` so the auth resume path can restore library or print context. Require a current user in the action itself, and if the request is unauthenticated, store the parsed intent plus `returnTo` in the generic pending-intent session contract from Plan 04-01 and redirect to `/account` so the original bookmark, list-CRUD, or membership write can replay after auth completes. For authenticated writes, return stable JSON payloads that at minimum echo `intent`, `ok`, and the affected `listId` and or `printSlug` when applicable, so fetchers can revalidate and show inline success states without inventing response semantics.</action>
  <verify>`pnpm test -- tests/accounts/save-actions.test.ts`, `pnpm typecheck`, and `pnpm build` pass, and signed-out bookmark, list-create, list-rename, list-delete, and membership attempts demonstrably redirect into auth with enough server state to resume the original write later.</verify>
  <done>Save and list writes have one honest server gate, one explicit route contract, and one resumable path through auth.</done>
</task>

<task type="auto">
  <name>Task 3: Wire fetcher save controls into discovery and print-detail surfaces</name>
  <files>app/root.tsx, app/components/discovery/DiscoveryCard.tsx, app/components/library/SavePrintButton.tsx, app/components/print-detail/PrintSaveActions.tsx, app/routes/print-detail.tsx</files>
  <action>Refactor the discovery card so print cards can expose a separate save control without nesting a button inside the card link, and keep generator cards out of the save flow. Add a reusable fetcher-driven save button with explicit labels such as `Save to Bookmarks` and `Saved to Bookmarks`, source the initial saved state from root or route loader data, and update the print-detail page so saving to `Bookmarks` is the primary save action while custom-list organization is a nearby secondary control. Keep these writes in place with fetchers rather than route navigation.</action>
  <verify>`pnpm lint`, `pnpm typecheck`, and `pnpm build` pass, and both discovery-card and print-detail save controls update inline with explicit text state after bookmark and list mutations.</verify>
  <done>Signed-in users can save from the surfaces that matter, and signed-out users get routed into auth without losing the original intent.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm prisma validate` passes
- [ ] `pnpm test -- tests/accounts/library-mutations.test.ts tests/accounts/save-actions.test.ts` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm build` passes
- [ ] `Bookmarks` has a one-per-user invariant and cannot be renamed or deleted through custom-list flows
- [ ] `/actions/save-print` and `/actions/list-membership` expose explicit mutation contracts for bookmark, list CRUD, and membership writes
- [ ] Discovery and print-detail save flows use fetchers with explicit text labels and auth-resume support
</verification>

<success_criteria>

- All tasks completed
- Signed-in users can bookmark prints from discovery and print-detail surfaces
- Signed-in users can create custom lists and change list membership without leaving the current page
- Signed-out bookmark, list-CRUD, and membership submissions redirect into auth and can resume the original write after successful sign-in or account creation
</success_criteria>

<output>
After completion, create `.planning/phases/04-accounts-and-personal-library/04-02-SUMMARY.md`
</output>
