---
phase: 01-foundation-and-content-model
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - prisma/schema.prisma
  - app/lib/env.server.ts
  - app/lib/db.server.ts
  - .env.example
  - docs/architecture/private-data-boundaries.md
  - tests/server/env.server.test.ts
autonomous: true
requirements:
  - PLAT-03
must_haves:
  truths:
    - "Private runtime configuration is loaded only from server-side code paths."
    - "The project has an explicit private runtime data model distinct from repo-backed public content."
    - "The repository documents where sensitive data may and may not live."
  artifacts:
    - path: "app/lib/env.server.ts"
      provides: "Server-only environment parsing and validation"
    - path: "app/lib/db.server.ts"
      provides: "Single server-side database entrypoint"
    - path: "prisma/schema.prisma"
      provides: "Private runtime schema baseline"
    - path: "docs/architecture/private-data-boundaries.md"
      provides: "Written rule set for public-versus-private data placement"
  key_links:
    - "Prisma runtime models represent private operational data and are not encoded into content files"
    - "Server-side env parsing is consumed through app/lib/db.server.ts and other server modules, not client-importable modules"
    - "The boundary documentation aligns with the actual code structure and env module names"
---

<objective>
Establish private runtime boundaries so the open-source repo does not become the accidental home for secrets or customer data.

Purpose: Create the first server-only env and runtime-data layer that later auth, library, and commerce features can build on safely.
Output: Prisma baseline, server-only env/db modules, and explicit privacy-boundary documentation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-content-model/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server-only environment parsing</name>
  <files>app/lib/env.server.ts, .env.example, tests/server/env.server.test.ts</files>
  <action>Define a single server-only environment module using TypeScript and runtime validation so secrets and private service configuration stay out of browser-importable code. Include only the minimum Phase 1 variables needed now, but structure the module so later auth, database, and commerce work extends it cleanly. Add a focused test for missing or malformed environment input behavior.</action>
  <verify>`pnpm test` passes for the env module test and `pnpm typecheck` passes with server-only imports.</verify>
  <done>The repo has a validated server-only env layer plus an `.env.example` that documents expected variables without leaking real values.</done>
</task>

<task type="auto">
  <name>Task 2: Add a private runtime database baseline</name>
  <files>prisma/schema.prisma, app/lib/db.server.ts</files>
  <action>Introduce Prisma as the runtime-private data boundary. Model only foundational private/runtime entities that are clearly justified by the roadmap, such as users, sessions, lists/bookmarks, and commerce intent, keeping the schema forward-compatible without overbuilding Phase 4 or Phase 6 in full. Do not duplicate repo-backed public print or generator content into the database just because Prisma exists.</action>
  <verify>`pnpm prisma validate` passes and the DB entrypoint typechecks cleanly.</verify>
  <done>The project has a private runtime schema and a single server-side DB access entrypoint ready for later phases.</done>
</task>

<task type="auto">
  <name>Task 3: Document and enforce the public/private boundary</name>
  <files>docs/architecture/private-data-boundaries.md</files>
  <action>Write the boundary contract that explains which data belongs in repo-backed content, which belongs in Prisma/runtime storage, and which must stay only in secrets/env. Make it concrete enough that future contributors can tell where customer data, payment metadata, API keys, creator records, and public print metadata belong. Keep the document aligned with the actual file structure introduced in this phase.</action>
  <verify>Manual doc review confirms the policy matches the content schema and Prisma/runtime module layout created in Plans `01-02` and `01-03`.</verify>
  <done>The project has an explicit, code-aligned privacy boundary document that future phases can follow instead of guessing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes for server/env tests
- [ ] `pnpm prisma validate` passes
- [ ] The privacy-boundary doc matches the actual content/runtime/env split in the repo
</verification>

<success_criteria>

- All tasks completed
- Private runtime data has a defined code path and schema boundary
- Server-only env handling exists and is tested
- Contributors can see, from code and docs, that public content and private runtime data live in different layers
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-content-model/01-03-SUMMARY.md`
</output>
