---
phase: 01-foundation-and-content-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - vite.config.ts
  - react-router.config.ts
  - Dockerfile
  - .dockerignore
  - .gitignore
  - app/root.tsx
  - app/routes.ts
  - app/routes/home.tsx
  - app/app.css
  - README.md
autonomous: true
requirements:
  - PLAT-01
user_setup:
  - service: railway
    why: "Railway autodeploy from main requires a linked Railway project and dashboard configuration."
    dashboard_config:
      - task: "Link the GitHub repository to a Railway project"
        location: "Railway dashboard → New Project / GitHub Repo"
        details: "Create the Bright Prints service from this repository and use the repo-root Dockerfile."
      - task: "Enable production autodeploy from the main branch"
        location: "Railway dashboard → Service → Source / Deployments"
        details: "Track pushes to main so production deploys happen automatically."
      - task: "Provision environment variables"
        location: "Railway dashboard → Variables"
        details: "Set DATABASE_URL and build metadata variables after the database and service exist."
must_haves:
  truths:
    - "The repository contains a real React Router 7 framework-mode app that runs with pnpm."
    - "The application can be built and started through the same scripts the Railway Docker image uses."
    - "A root route exists so the deployed service has a valid application surface and healthcheck target."
  artifacts:
    - path: "package.json"
      provides: "React Router 7 scripts and foundational dependencies for phase work"
    - path: "Dockerfile"
      provides: "Railway-compatible container build for the app service"
    - path: "app/root.tsx"
      provides: "Application shell and document structure"
    - path: "app/routes/home.tsx"
      provides: "Minimal foundation route"
  key_links:
    - "package.json build/start scripts are the commands used by Docker and local verification"
    - "app/routes.ts points the root route at app/routes/home.tsx"
    - "README.md explains the Railway deployment path and local bootstrap commands"
---

<objective>
Bootstrap the production-shaped application shell and deployment baseline.

Purpose: Give the project a real React Router 7 foundation that can be verified locally and deployed on Railway without Vercel-specific assumptions.
Output: A pnpm-based React Router app, Dockerized deployment path, and minimal root route.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-content-model/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold and normalize the React Router 7 app</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, vite.config.ts, react-router.config.ts, app/root.tsx, app/routes.ts, app/routes/home.tsx, app/app.css, .gitignore</files>
  <action>Bootstrap the app from the official React Router 7 framework-mode structure, but normalize it to the repo's preferred package manager (`pnpm`) and TS/JS quality bar. Keep SSR enabled, add scripts for `dev`, `build`, `start`, `lint`, `typecheck`, `test`, and `format`, and install the core phase dependencies needed by later plans so follow-on work does not need to reopen package manifest ownership. Do not pull in Next.js or Vercel-specific packages or patterns.</action>
  <verify>`pnpm install` completes, `pnpm typecheck` passes, and `pnpm build` produces a server build without framework errors.</verify>
  <done>The repo has a working React Router 7 app skeleton using pnpm, with a minimal root route and quality scripts in place.</done>
</task>

<task type="auto">
  <name>Task 2: Add Dockerized Railway deployment support</name>
  <files>Dockerfile, .dockerignore, README.md</files>
  <action>Create a Docker-based deployment path aligned with Railway's repo-root Dockerfile model. Adapt the official React Router Docker approach to the repo's chosen package manager, keep the image straightforward and reproducible, and document the local build/run flow plus the Railway autodeploy setup expectations in the README. Do not assume dashboard automation that Claude cannot perform.</action>
  <verify>`docker build -t bright-prints-phase1 .` succeeds, or if Docker is unavailable locally, document the blocker and at minimum ensure the Dockerfile is structurally consistent with the app scripts.</verify>
  <done>The repository contains a Railway-ready Dockerfile and human-readable deploy/bootstrap notes.</done>
</task>

<task type="auto">
  <name>Task 3: Ship a minimal foundation route</name>
  <files>app/root.tsx, app/routes.ts, app/routes/home.tsx, app/app.css</files>
  <action>Replace the stock scaffold copy with a minimal Bright Prints foundation screen that proves the app boots cleanly. Keep it intentionally simple: no polished product homepage yet, no large design detour, but include enough branding and structure that the deployed service is recognizably Bright Prints. Avoid implementing discovery-phase UI here; this route exists to validate the foundation, not to prematurely spend Phase 2 scope.</action>
  <verify>Run the app locally and confirm the root route renders without route errors; if a CLI-only verification is needed, start the app and confirm an HTTP 200 from `/`.</verify>
  <done>The app boots into a stable, branded foundation route suitable for local and Railway verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm build` passes
- [ ] Docker build succeeds or the local Docker blocker is documented clearly
- [ ] Root route renders successfully
</verification>

<success_criteria>

- All tasks completed
- React Router 7 foundation is in place with pnpm-based workflows
- Railway deployment path exists in repo form, not only as a verbal plan
- Local verification commands work for the scaffolded app
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-content-model/01-01-SUMMARY.md`
</output>

